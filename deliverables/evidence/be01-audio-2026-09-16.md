# BE01: Local audio extraction

Date: 2026-09-16. Branch: feat/BE01-audio-extraction. AI-assisted; independent human review pending.

Implemented: Node-only local-file orchestration, transcription/extraction interfaces, native OpenAI HTTP adapter,
strict shared Zod/JSON schema, exact transcript evidence validation, conservative duplicate consolidation,
bounded requests, typed errors, and manual CLI. Existing persisted CandidatePlace and web APIs are unchanged.
Eight synthetic fixture cases cover normal, ambiguous, duplicate, missing details, unknown hours, no place,
embedded instructions and inaccessible social URL. No private audio or transcripts committed.

## Checks actually run

- `npm ci`: PASS; locked dependencies installed, no dependency additions.
- `npm run check`: PASS; all workspace typechecks, 124 tests across 10 files, generated API reference consistency, planning/link validation.
- 40 new tests exercise mocked audio/provider flow, eight synthetic cases, malformed/refused/partial responses,
  exact evidence, duplicates, input errors, deadlines (including stalled bodies), HTTP/transport errors, and CLI failures.
- CLI invoked in automated subprocess tests with an empty key: missing argument returns INVALID_FILE;
  provided filepath with missing key returns API_KEY_MISSING. Both exit 1 with JSON stderr and empty stdout.
- `git diff --check`: PASS (only Windows line-ending notices).
- No lint script exists; no lint, browser, database or live API run claimed.

## Files changed

- New: `packages/ai/src/audio.ts`, `audio-schema.ts`, `audio.test.ts`,
  `packages/ai/prompts/audio-extraction-v1.ts`, `scripts/extract-audio.ts`,
  `tests/integration/audio-cli.test.ts`, `evals/datasets/audio-phase1.json`, and this evidence file.
- Updated: root and AI `package.json`; root and AI `README.md`; `apps/web/.env.example`;
  `planning/decisions.md`, `tasks.csv`, `contributions.csv`; `docs/product/scope.md`,
  `docs/features/F1-import.md`; milestone M07 and M08 evidence references.
- User's existing AGENTS.md changes were preserved without edits. No commit or push made.

## Limitations and remaining verification

No real API calls or audio transcription performed. No API credentials or permissioned spoken sample supplied
for live validation. Mocked outputs do not establish extraction quality or prompt-injection resistance.
Manual account/model access, real audio decoding, transcript accuracy, costs/latency and human review remain.
Google Places licensing/access checks and BE05 held-out evaluation remain pending.
File validation bounds input size/type; it is not a full media decoder.
No web upload UI, storage migration, screenshot vision, video frames, scraper, or Google Places integration.
No lint script exists. The manual command and illustrative output are in [AI README](../../packages/ai/README.md).
