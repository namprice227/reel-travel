# PR 28 merge resolution — 2026-09-24

Merged main at e4e483d into feat/tick-places-auto-route.

- Retained the single early multimodal screenshot path, shared candidate persistence, source-day hints, lookup-cap uncertainty, and link recovery from traveler details. Removed the duplicate screenshot path introduced by combining the branches.
- Kept strict stop request schemas and reel-format fields alongside main’s image extraction prompt, with one prompt declaration.
- Preserved both branches’ contributions and decisions, landing-page evidence, and extraction benchmark task progress.
- Removed the duplicate analyze:image package script introduced by the automatic merge.

Validation: npm run check passed locally: 777 tests across 72 files, workspace typechecking, generated API reference and planning validation. GitHub CI additionally runs database tests against disposable PostgreSQL and a production build; its result is recorded on PR 28. No live provider calls or production migrations were run for this resolution.

CI initially exposed the old 11-table assertion after the account-library migration added three tables. Updated the expected total to 14 and added explicit anonymous/authenticated access-denial checks for all three new tables; retained the RLS assertion for every table. The final CI run verifies this correction.
