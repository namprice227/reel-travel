# GA4 analytics integration — 22 September 2026

User supplied web-stream ID `G-1MNPG57MNM` and confirmed Enhanced measurement was turned off.
This integration measures production pathname navigation plus browser-observed trip creation, import
submission/retry, place addition/confirmation, generation/regeneration, edits, sharing and action failures.
It uses the same typed API client as the UI so successful events follow successful responses; dry-run
edits and polling do not count as product actions. Server/worker webhook events are not represented as
GA4 completion events without a corresponding browser observation.

Trip/place IDs, share tokens, query strings, free-text form fields and response contents are omitted from
explicit GA4 payloads. Route templates aggregate pages. Local development is excluded, while opt-in debug
traffic is labeled. No Vercel Web Analytics dependency remains. Google Analytics uses cookies and device
information; the privacy page was updated. CSP allows the tag and its analytics destinations.

## Verification

- `npm run check`: PASS, 577 tests across 52 files, typechecks, API docs and workspace validation.
- `npm run build`: PASS.
- Six focused synthetic GA4 tests verify URL redaction, SPA navigation deduplication, generation mode,
  start/success/failure separation, bounded event properties, dry-run/polling exclusion and non-blocking delivery.
- These tests mock the browser tag and do not prove events arrived in Google's property.
- Browser-control access was unavailable in this session. Realtime/DebugView and report screenshot
  verification remain explicitly pending until observed, not inferred from a successful build.

## Report and product decisions

[Report instructions and event dictionary](../../docs/operations/ga4.md) explain the required GA4 setting,
Realtime/DebugView checks, test-traffic exclusion, 24–48 hour processing window, report screenshots,
custom dimensions and same-session funnel explorations.

No real-user behavioral findings are claimed at integration time. M19 needs a dated screenshot, sample
size, observed metrics and a justified product decision after testers use the deployed tracker. The guide
provides hypotheses for add-place drop-off, generation failures, regeneration, editing, sharing and mobile
usage; these are proposed analyses rather than invented results.
